<?xml version="1.0" encoding="UTF-8"?>
<!--
    TaskHive Logging Configuration (NFR-MAIN-07 + NFR-MAIN-08)
    - Dev profile: colored console output for readability
    - Prod profile: structured JSON for ELK/Splunk/CloudWatch
    - Rolling file appenders with retention per SRS:
        ERROR: 90 days | WARN/INFO: 30 days | DEBUG: 7 days
-->
<configuration>

    <!-- Log redaction converter (NFR-SEC-12): redacts emails, JWTs, password fields -->
    <conversionRule conversionWord="redact"
                    converterClass="com.digiwork.taskhive.common.logging.LogRedactionConverter"/>

    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="taskhive-backend"/>

    <!-- ===== COMMON PROPERTIES ===== -->
    <property name="LOG_DIR" value="./logs"/>
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{correlationId:-}] %-5level %logger{36} - %redact%n"/>

    <!-- ===== DEV PROFILE: Console with colors ===== -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %highlight(%-5level) [%blue(%thread)] [%magenta(%X{correlationId:-N/A})] %cyan(%logger{36}) - %redact%n</pattern>
            </encoder>
        </appender>

        <!-- Dev file output (all levels, plain text) -->
        <appender name="DEV_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_DIR}/taskhive-dev.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_DIR}/archive/taskhive-dev.%d{yyyy-MM-dd}.log</fileNamePattern>
                <maxHistory>7</maxHistory>
                <totalSizeCap>500MB</totalSizeCap>
            </rollingPolicy>
            <encoder>
                <pattern>${LOG_PATTERN}</pattern>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="DEV_FILE"/>
        </root>

        <logger name="com.digiwork.taskhive" level="DEBUG"/>
        <logger name="org.springframework.security" level="DEBUG"/>
    </springProfile>

    <!-- ===== PROD PROFILE: Structured JSON + retention policies ===== -->
    <springProfile name="prod,staging">

        <!-- JSON Console (for container stdout → log aggregator) -->
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>correlationId</includeMdcKeyName>
                <customFields>{"app":"${APP_NAME}","env":"${SPRING_PROFILES_ACTIVE:-prod}"}</customFields>
                <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSS'Z'</timestampPattern>
            </encoder>
        </appender>

        <!-- ERROR log file: 90-day retention (NFR-MAIN-08) -->
        <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_DIR}/taskhive-error.log</file>
            <filter class="ch.qos.logback.classic.filter.LevelFilter">
                <level>ERROR</level>
                <onMatch>ACCEPT</onMatch>
                <onMismatch>DENY</onMismatch>
            </filter>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_DIR}/archive/error/taskhive-error.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
                <maxHistory>90</maxHistory>
                <totalSizeCap>5GB</totalSizeCap>
            </rollingPolicy>
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>correlationId</includeMdcKeyName>
                <customFields>{"app":"${APP_NAME}"}</customFields>
            </encoder>
        </appender>

        <!-- WARN + INFO log file: 30-day retention (NFR-MAIN-08) -->
        <appender name="WARN_INFO_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_DIR}/taskhive-app.log</file>
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>INFO</level>
            </filter>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_DIR}/archive/app/taskhive-app.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
                <maxHistory>30</maxHistory>
                <totalSizeCap>3GB</totalSizeCap>
            </rollingPolicy>
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>correlationId</includeMdcKeyName>
                <customFields>{"app":"${APP_NAME}"}</customFields>
            </encoder>
        </appender>

        <!-- DEBUG log file: 7-day retention (NFR-MAIN-08) -->
        <appender name="DEBUG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_DIR}/taskhive-debug.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>${LOG_DIR}/archive/debug/taskhive-debug.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
                <maxHistory>7</maxHistory>
                <totalSizeCap>1GB</totalSizeCap>
            </rollingPolicy>
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdcKeyName>correlationId</includeMdcKeyName>
                <customFields>{"app":"${APP_NAME}"}</customFields>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
            <appender-ref ref="ERROR_FILE"/>
            <appender-ref ref="WARN_INFO_FILE"/>
        </root>

        <logger name="com.digiwork.taskhive" level="INFO">
            <appender-ref ref="DEBUG_FILE"/>
        </logger>

        <!-- Suppress noisy frameworks in prod -->
        <logger name="org.springframework" level="WARN"/>
        <logger name="org.hibernate" level="WARN"/>
        <logger name="org.apache" level="WARN"/>
    </springProfile>

</configuration>
